# 更新日志

本项目的所有重要变更都将记录在此文件中。

## [未发布]

### 新增 (Added)
- **统一 CLI (`govector`)**: 将 `govectord` 和 `govector-server` 合并为一个综合性的命令行工具。
- **交互式 TUI**: 在无参数运行 `govector` 时，默认进入带有绿色主题的终端用户界面（TUI），包含自定义的 ASCII 标志和 `/?` 帮助菜单。
- **`rm` 命令**: 引入了用于从数据库中安全删除集合的命令，并附带明确的安全机制，防止意外删除 "default" 默认集合。
- **自动创建**: 执行写操作（如 `upsert` 和 TUI 启动）时，如果指定的数据库文件和集合不存在，现在会自动进行创建，并默认启用 HNSW 索引。
- **存在性校验**: 读操作（如 `search`, `count`, `delete`）现在会严格校验数据库和集合是否存在，若不存在则优雅报错并退出。
- **动态版本控制**: 在构建和发布流水线（`Makefile`, `build_release.sh`）中集成了 `git describe --tags` 来动态解析版本号，取代了硬编码的版本字符串。

### 变更 (Changed)
- **CLI 语法**: 将 CLI 命令行语法更新为 `govector <command> [dbfile] [options]`，使得数据库文件参数变为可选（默认使用 `govector.db`）。
- **服务器关停**: 在 TUI 中运行 `serve` 命令时，现在能够优雅地处理 `Ctrl+C` 中断信号，停止 HTTP 服务器并返回到 TUI 提示符，而不是直接终止应用程序。
- **发布脚本**: 更新了 Homebrew 的 `.rb` 部署脚本以及 `.service` 服务模板，统一使用 `govector serve` 命令，并移除了遗留的标志（如 `-hnsw=true`）。
- **文档同步**: 同步了所有的全局文档，包括 `README.md`, `README_zh.md`, `.docs/` 和 `.qoder/repowiki/` 中的内容，以匹配全新的 CLI 架构。

### 移除 (Removed)
- **`cmd/govector-server`**: 完全删除了独立的服务器入口点，改为使用新的 `govector serve` 子命令。
- **`govectord`**: 从 Homebrew 的构建流水线中移除了 `govectord` 二进制文件的别名。

## [0.2.0] - 2026-03-12

### 新增 (Added)
- **Protobuf 存储引擎**: 使用 Protocol Buffers 替代 JSON 序列化，实现高性能的向量和数据点持久化。
- **集合自动发现**: API 服务器在启动时，现在会自动扫描并注册 BoltDB 存储中发现的所有集合。
- **数据版本控制**: 为所有数据点引入了 64 位纳秒级精度的版本控制，为未来的数据一致性检查提供支持。
- **SQ8 向量量化**: 集成了 8 位标量量化 (Scalar Quantization) 技术，显著降低大规模数据集的磁盘空间占用。
- **动态集合元数据**: 支持持久化和加载特定于集合的参数（如维度数、距离度量方法、HNSW 配置）。
- **并发访问安全**: 对 API 服务器的集合注册表提供了完整的互斥锁保护，消除了数据竞争问题。
- **高级 Payload 过滤**: 在搜索和删除操作中新增了对 `Range` (范围), `Prefix` (前缀), `Contains` (包含) 和 `Regex` (正则) 过滤条件的支持。
- **一致性删除**: 重构了删除逻辑，确保存储和内存索引始终保持同步（遵循"存储优先，后内存"的原则）。
- **大规模基准测试套件**: 新增基准测试工具，支持 1万、10万、100万 和 1000万级别的数据规模测试，并报告内存使用情况。

### 优化 (Optimized)
- **HNSW 召回率**: 改进了过滤搜索的后处理召回启发式算法，通过在初步搜索时获取更多的候选节点来提升召回效果。
- **测试覆盖率**: 将核心（core）包的测试覆盖率提高至 **92.7%**，API 包的覆盖率提高至 **92.8%**。
- **内存管理**: 在大规模数据集下的基准测试中引入批处理操作并优化了垃圾回收 (GC) 的触发机制。

### 修复 (Fixed)
- 修复了 `api/server.go` 在启动/停止 HTTP 服务器时可能引发的数据竞争问题。
- 修复了 SQ8 量化算法中潜在的整数溢出错误。
- 修复了通过 API 创建的集合在服务器重启后会丢失的漏洞。
